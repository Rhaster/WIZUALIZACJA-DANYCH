
def cos(a=input("podaj")):
    a.lower()
    g=input("podaj nazwe")
    t=a.replace('.','').replace(',','').split(" ")
    J=int(len(t))
    H=J
    print(t)
    print(J)
    print(t[1])
    ile=0
    for x in t:
        for j in t                                                                  :
            if x==j:
                g.remove(x)
                cos+=1
        print(g)
        for x in range(0,(y-1)-cos):
            d.writelines(g[x])
            d.writelines("\n")
        print(g)
    ##with open(g,"a") as no:
cos()